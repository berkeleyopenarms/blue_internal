def getWaypoints():
	waypoints = [[-1, 1, .2], [1, 1, .2]] # Each element in the list is a list of coordinates like [x, y, z]
	return waypoints

def getPaths():
	

if __name__ == '__main__':
	waypoints = getWaypoints()

	paths = getPaths()
